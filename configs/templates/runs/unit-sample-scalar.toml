# Small scalar run template.
# Add tasks from the dashboard task templates after creating the run.
name = "unit-sample-scalar"

[evaluator]
kind = "unit"
continuous_dims = 1
discrete_dims = 0

[parametrization]
kind = "identity"
