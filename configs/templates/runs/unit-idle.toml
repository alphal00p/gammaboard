# Minimal idle run.
# Good starting point when you want to create a run first and append tasks later.
name = "unit-idle"

[evaluator]
kind = "unit"
continuous_dims = 1
discrete_dims = 0

[parametrization]
kind = "identity"
