# Real-valued Monte Carlo sampling with the default identity materializer.

[[task_queue]]
kind = "sample"
nr_samples = 1_000
[task_queue.config]
observable = "scalar"

[task_queue.config.sampler_aggregator]
kind = "naive_monte_carlo"
