name = "symbolica-test"
target = { kind = "scalar", value = 5.568327996831708 }

[evaluator]
kind = "symbolica"
expr = "exp(-x^2 -y^2-z^2)"
args = ["x", "y", "z"]

[sampler_aggregator]
kind = "havana"
seed = 0
bins = 64
min_samples_for_update = 1024
samples_for_update = 16384
stop_training_after_n_samples = 65536
initial_training_rate = 0.1
final_training_rate = 0.001

[parametrization]
kind = "spherical"

[evaluator_runner_params]
min_loop_time_ms = 0
performance_snapshot_interval_ms = 2000

[sampler_aggregator_runner_params]
min_poll_time_ms = 100
performance_snapshot_interval_ms = 2000
target_batch_eval_ms = 400.0
target_queue_remaining = 0.5
lease_ttl_ms = 5000
max_batch_size = 64
max_batches_per_tick = 16
max_queue_size = 128
completed_batch_fetch_limit = 1024
