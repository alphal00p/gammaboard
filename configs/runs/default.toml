# Shared defaults merged into every `gammaboard run add <config.toml>` payload.
# `name` is intentionally omitted and must be provided per run.
[evaluator]
kind = "unit"
continuous_dims = 1
discrete_dims = 0

[evaluator_runner_params]
performance_snapshot_interval_ms = 2000

[sampler_aggregator_runner_params]
performance_snapshot_interval_ms = 2000
aggregation_persist_interval_ms = 2000
target_batch_eval_ms = 500.0
queue_buffer = 1.5
max_batch_size = 1024
max_queue_size = 4096
max_batches_per_tick = 64
completed_batch_fetch_limit = 128
strict_batch_ordering = true
