# TODO

## Platform
- [ ] implement `madnis` sampler-aggregator as a parametrization
- [ ] add gammaloop observable, with possibly custom panel on frontend
- [ ] expose sampler enqueue-drain wait explicitly in performance metrics so hidden sampler tick loss is visible
- [ ] instrument and optimize `insert_batches` end to end, especially the `batch_inputs` write path
