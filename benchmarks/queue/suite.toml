# Compact default: all fleet sizes and both ends of the compute-capacity range.
# Single-binary and paired A/B invocations have a 300-second wall-time budget.
evaluators = [1, 4, 16, 64]
rates = [1000, 2000000]
regimes = ["inference", "training_burst"]
continuous_dims = 6
noise_fraction = 0.0
warmup_seconds = 0.5
measurement_seconds = 4
repetitions = 1
