# Backend API/dashboard and deploy settings for the UBELIX profile.
name = "ubelix"
allow_local_node_spawn = false

[frontend]
build_dir = "/usr/local/share/gammaboard/dashboard"
host = "0.0.0.0"

# Supply [auth] through a private server config when dashboard authentication
# is required on the cluster network.
