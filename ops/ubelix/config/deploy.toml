# Foreground deploy orchestration for UBELIX control jobs.
[api_server]
api_server_config = "server.toml"

[static_site]
frontend_build_dir = "/usr/local/share/gammaboard/dashboard"

[frontend_http]
frontend_host = "0.0.0.0"
frontend_port = 8080
frontend_server_name = "_"
